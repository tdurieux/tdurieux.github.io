\begin{table}
  \centering
  \caption{Patch Regression Results}
  \label{tab:regression}
  \resizebox{0.45\textwidth}{!}{
  \begin{tabular}{|l|l|r|l|r|}
    \hline
    Project & Patch Location  & \rotatebox{90}{Patch ID}  & \rotatebox{90}{\tabincell{c}{Valid/Total \\ Request}}  & \rotatebox{90}{\# Applied} \\
    \hline {% for project, valueProject in data %}{% for location, value in valueProject %}{% for location, patch_id, agregated in value %}{% if location != 'original'%}
    {{ project.title() }} & {{ location }} & {{ patch_id }} & {{ agregated.nbRequest - agregated.nbDiff }}/{{ agregated.nbRequest}} & {{agregated.appliedPatches|length}} \\{% endif %}{% endfor %}{% endfor %}
    \hline{% endfor %}
    {{data|length}} projects & {% raw %}\multicolumn{4}{|l|}{{% endraw %}{{ countTotal }} patches and {{ countValid }} valid patches} }}\\
    \hline
  \end{tabular}
  }
\end{table}